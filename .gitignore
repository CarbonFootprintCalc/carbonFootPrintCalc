node_modules/

# amplify
.amplify
amplify_outputs*
amplifyconfiguration*

# Lock files (keep only the relevant one)
pnpm-lock.yaml  # Ignore if using npm/yarn instead
yarn.lock       # Ignore if using npm/pnpm instead
# Do NOT ignore package-lock.json if using npm
